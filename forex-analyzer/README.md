# Forex Analyzer

Приложение для анализа валютных пар на форексе и отправки сигналов в Telegram.

## Описание

Приложение проверяет валютные пары за 10 минут до окончания каждого часа и отправляет сигналы:

- **BUY (Покупка)**: если последняя дневная свеча зеленая И последние 2 часовых свечи зеленые
- **SELL (Продажа)**: если последняя дневная свеча красная И последние 2 часовых свечи красные

## Технологии

- Next.js 14
- TypeScript
- React
- Node.js
- Telegram Bot API

## Установка

```bash
cd forex-analyzer
npm install
```

## Настройка

1. Создайте файл `.env.local` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

2. Получите токен бота:
   - Откройте @BotFather в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям

3. Получите Chat ID:
   - Добавьте бота в чат или канал
   - Отправьте сообщение
   - Используйте @userinfobot или аналогичный для получения ID

## Использование

### Запуск веб-интерфейса

```bash
npm run dev
```

Откройте http://localhost:3000 в браузере.

### Ручной запуск анализа

```bash
npm run analyze
```

### Автоматический запуск (cron)

Добавьте в crontab (`crontab -e`):

```bash
# Запуск каждые 10 минут до окончания часа (в :50)
50 * * * * cd /path/to/forex-analyzer && /usr/bin/npm run analyze >> /var/log/forex-analyzer.log 2>&1
```

### API endpoint

GET запрос на `/api/analyze`:

```bash
curl http://localhost:3000/api/analyze
```

## Структура проекта

```
forex-analyzer/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── analyze/
│   │   │       └── route.ts      # API endpoint
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   └── page.tsx              # Веб-интерфейс
│   ├── lib/
│   │   ├── telegram.ts           # Логика Telegram и анализа
│   │   └── utils.ts              # Вспомогательные функции
│   ├── scripts/
│   │   └── analyzer.ts           # Скрипт для cron
│   └── types/
│       └── index.ts              # TypeScript типы
├── .env.local                    # Переменные окружения
├── package.json
├── tsconfig.json
└── README.md
```

## Логика работы

1. Скрипт получает данные свечей с API (Binance для демонстрации)
2. Анализирует последнюю дневную свечу
3. Анализирует последние 2 часовых свечи
4. Если все свечи одного цвета - формирует сигнал
5. Отправляет сообщение в Telegram

## Пример сообщения в Telegram

```
🟢 ПОКУПКА BTCUSDT

📊 Анализ свечей:
• Дневная свеча: 🟢 Зеленая
• Часовые свечи: 🟢 Зеленые (последние 2)

⏰ Время сигнала: 17.04.2024 15:50:00

💡 Это автоматический сигнал. Проведите собственный анализ перед торговлей!
```

## Примечания

- Для реального Forex используйте специализированный API (например, OANDA, FXCM, Alpha Vantage)
- В текущей версии используются крипто-пары через Binance API для демонстрации
- Всегда проводите собственный анализ перед торговлей
- Автор не несет ответственности за финансовые потери

## Лицензия

MIT
